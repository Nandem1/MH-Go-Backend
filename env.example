# Database Configuration
DATABASE_URL=postgres://user:password@localhost:5432/stock_db

# Redis Configuration
REDIS_URL=redis://localhost:6379

# Server Configuration (opcional - tiene valores por defecto)
PORT=8080
GIN_MODE=release

# JWT Configuration (opcional - tiene valores por defecto)
JWT_SECRET=your-super-secret-jwt-key-change-in-production

# Logging (opcional - tiene valores por defecto)
LOG_LEVEL=info 